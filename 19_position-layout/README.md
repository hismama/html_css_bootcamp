# Position
* Fixed - exact placement on the page
* Relative - relative to itself based on where it would have been
* Absolute - position in relationship to the next element up the DOM
* Static - everything is (default in doc flow)

***
**top, right, bottom**, and **left** properties specify the position of positioned elements
